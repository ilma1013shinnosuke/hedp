# ② 情報蓄積

## 目的

①で得た事実を失わず、後から再現・確認・分析できる形で保存する。大まかには
「証拠を残す」と「使える形で残す」の両方を担当する。

## 保存する種類

- RawData: メーカーの完全応答と取得条件を、変更しない証拠として保存する。
- Record: RawDataから再現できる、項目名・値・単位をそろえたデータを保存する。
- 現在値: 最後に確認できた状態と、その取得時刻・鮮度を示す。
- 時系列履歴: 値や状態の変化を時刻順に保持する。
- イベント: センサー反応、アラーム、操作、失敗など一回ごとの出来事を保持する。
- 状態区間: 照明、鍵、運転mode等が、いつからいつまで同じ状態だったかを保持する。
- 観測可能性区間: 正常観測、不安定、観測不能だった期間と、その判定根拠を保持する。

## 原則

- 欠損を0で埋めない。推測値を観測事実として保存しない。
- RawDataは保持中に上書き、変更しない。圧縮移行と削除は共通保存方針の削除ゲートを通す。
- 正規化規則を直した場合、RawDataから再構築できるようにする。
- 同一事実の重複は防ぐ。異なる取得時刻の同一現在値は直近証拠として保持できるが、
  長期保存は変化密度と分析価値で決める。
- 離散状態の同一値を繰り返し保存せず、現在値と状態区間を更新する。定期取得の成功・失敗列は
  短期証拠とし、長期には観測可能性区間と障害・復旧eventへ集約する。
- 想定外値は`invalid`または`unknown`として値を`null`にし、正常値や前回値で隠さない。
  必要なRawは秘密を通常logへ出さず、容量と期間に上限のある隔離保存へ送る。
- 値だけでなく取得元、品質、鮮度、変換方法を追跡できるようにする。
- 保存期間、粒度、可逆圧縮、削除条件は
  [共通保存方針](data-retention-policy.md)に従う。

## 現在の実装

共通のRawData、Record、SQLite保存は `src/hedp/storage/` にある。SwitchBotは専用の
機器台帳、観測、取込監査、競合、欠損、集計テーブルも持つ。統合のためだけに既存DB
構造を変更せず、実際の利用要件が明確になった時点で共通化を判断する。

## 容量

現行RawDataは `hedp.db` 内にある。整理作業では無検証の複製、移動、削除をしない。
保存期間、圧縮、別ストレージ化は、復元可能性と必要な履歴を確認する独立作業として行う。
DB全体のgzipバックアップは災害復旧用であり、年月別Rawアーカイブの代わりにしない。
